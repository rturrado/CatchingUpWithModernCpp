#include <iostream>

#include "BitSequenceToInt.h"
#include "LogSystemWithCountingSemaphores.h"
#include "ParseIniFile.h"
#include "ParseIniFileWithBoost.h"
#include "ParseLinesOfDatesAndTimes.h"
#include "ParseTimeStringsToDurationsAndCompare.h"
#include "PlayingWithTimeZonesAndDst.h"
#include "PrintTimetInLocalTime.h"
#include "ReadFileToBuffer.h"
#include "ThreadAfterTimer.h"
#include "VectorOfIntsWithTrianglePattern.h"

import ParsingDates;
import PopenTest;

int main()
{
/*
    // Bit sequence to int
    { std::cout << "[BIT SEQUENCE TO INT]\n"; bit_sequence_to_int_main(); std::cout << "\n"; }
    // Log system with counting semaphores
    { std::cout << "[LOG SYSTEM WITH COUNTING SEMAPHORES]\n"; log_system_with_counting_semaphores_main(); std::cout << "\n"; }
    // Parse INI file
    { std::cout << "[PARSE INI FILE]\n"; parse_ini_file_main(); std::cout << "\n"; }
    // Parse INI file with Boost
    { std::cout << "[PARSE INI FILE WITH BOOST]\n"; parse_ini_file_with_boost_main(); std::cout << "\n"; }
    // Parse lines of dates and times
    { std::cout << "[PARSE LINES OF DATES AND TIMES]\n"; parse_lines_of_dates_and_times_main(); std::cout << "\n"; }
    // Parsing dates
    { std::cout << "[PARSING DATES]\n"; parsing_dates_main(); std::cout << "\n"; }
    // Parsing time strings to durations and compare
    { std::cout << "[PARSING TIME STRINGS TO DURATIONS AND COMPARE]\n"; parse_time_strings_to_durations_and_compare_main(); std::cout << "\n"; }
    // Playing with time zones and DST
    { std::cout << "[PLAYING WITH TIME ZONES AND DST]\n"; playing_with_time_zones_and_dst_main(); std::cout << "\n"; }
    // Popen test
    { std::cout << "[POPEN TEST]\n"; popen_test_main(); std::cout << "\n"; }
    // Read file to buffer
    { std::cout << "[READ FILE TO BUFFER]\n"; read_file_to_buffer_main(); std::cout << "\n"; }
    // Thread after timer
    { std::cout << "[THREAD AFTER TIMER]\n"; thread_after_timer_main(); std::cout << "\n"; }
    // Vector of ints with triangle pattern
    { std::cout << "[VECTOR OF INTS WITH TRIANGLE PATTERN]\n"; vector_of_ints_with_triangle_pattern_main(); std::cout << "\n"; }
*/
    // Print time_t in local time
    { std::cout << "[PRINT TIME_T IN LOCAL TIME]\n"; print_time_t_in_local_time_main(); std::cout << "\n"; }
}
